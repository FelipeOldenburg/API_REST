# Domain Docs

Antes de explorar, leia `CONTEXT.md` ou `CONTEXT-MAP.md` e ADRs relevantes em `docs/adr/`, quando existirem. Na ausência deles, prossiga silenciosamente.

Este é um repositório single-context. Use o vocabulário definido em `CONTEXT.md` e sinalize conflitos com ADRs existentes.
